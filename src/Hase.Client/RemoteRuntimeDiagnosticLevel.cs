namespace Hase.Client;

/// <summary>Identifies the detail level of a remote Runtime Host diagnostic.</summary>
public enum RemoteRuntimeDiagnosticLevel
{
    Operational = 0,
    Protocol = 1,
    Bytes = 2
}
