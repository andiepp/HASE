# ADR-0034 Increment 9 Fix 1 — Descriptor Layout

## Scope

Adds the missing spacer and content rows for the instrument section in the
endpoint-details grid.

The instrument heading and cards now occupy their own row below the endpoint
description.

No ViewModel, runtime, descriptor, transport, or northbound behavior changes.
